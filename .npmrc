# Avoid interactive prompts / TTY failures when pnpm verifies deps before scripts
confirm-modules-purge=false

# Puppeteer is only used for local screenshot scripts — skip its postinstall
ignore-dep-scripts=false
